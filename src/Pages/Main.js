import React from "react";
import { HoverButton } from "../Components/HoverButton";

const Main = () => {
  return (
    <div>
      <HoverButton>aaa</HoverButton>
    </div>
  );
};

export default Main;
